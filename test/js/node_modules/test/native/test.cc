#include <node.h>

using namespace v8;

int test(const FunctionCallbackInfo<Value>& args) {
	args.GetReturnValue().Set(v8::Number::New(args.GetIsolate(), 1000));
}

// serial_number

// static void InitTest(v8::Local<v8::Template> recv,
// 									Local<Value> unused,
// 									Local<Context> context, void* priv) {
//   NODE_SET_METHOD(recv, "test", test);
// }

// NODE_MODULE(_test, InitTest)
